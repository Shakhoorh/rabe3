RewriteEngine On

# Redirect from the root to home.php
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^(.*)$ /home.php [L,R=301]

# Remove .php extension from URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^(.*)$ $1.php [L]
